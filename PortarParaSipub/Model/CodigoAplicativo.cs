namespace PlataformaOperacional.Model.Plataforma
{
    // ATENÇÃO: se a Plataforma Operacional já possui um enum equivalente que
    // identifica os módulos/aplicativos, use o existente e remova este arquivo
    // (ajustando a propriedade CodigoAplicativo em MensagemNotificacao e NotificacaoDto).
    public enum CodigoAplicativo
    {
        Sipub,
        Geral,
        Amortizacao,
        EncontroDeContas
    }
}
